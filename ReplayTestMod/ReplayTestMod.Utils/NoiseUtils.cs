using Cinemachine;
using UnityEngine;

namespace ReplayTestMod.Utils
{
    public static class NoiseUtils
    {
        /*
        public static NoiseSettings CreateCustomNoiseProfile()
        {
            var noise = ScriptableObject.CreateInstance<NoiseSettings>();

            noise.PositionNoise = new NoiseSettings.TransformNoiseParams[3];
            noise.OrientationNoise = new NoiseSettings.TransformNoiseParams[3];

            noise.PositionNoise[0] = new NoiseSettings.NoiseParams
            {
                Amplitude = new Vector3(1f, 0.5f, 0.2f),
                Frequency = new Vector3(0.3f, 0.3f, 0.3f)
            };

            noise.PositionNoise[1] = new NoiseSettings.NoiseParams
            {
                Amplitude = new Vector3(0.4f, 0.2f, 0.1f),
                Frequency = new Vector3(1.5f, 1.5f, 1.5f)
            };

            noise.PositionNoise[2] = new NoiseSettings.NoiseParams
            {
                Amplitude = new Vector3(0.1f, 0.1f, 0.1f),
                Frequency = new Vector3(8f, 8f, 8f)
            };

            noise.OrientationNoise[0] = new NoiseSettings.NoiseParams
            {
                Amplitude = new Vector3(0.4f, 0.4f, 0.4f),
                Frequency = new Vector3(0.5f, 0.5f, 0.5f)

            };

            noise.OrientationNoise[1] = new NoiseSettings.NoiseParams
            {
                Amplitude = new Vector3(0.2f, 0.2f, 0.2f),
                Frequency = new Vector3(2f, 2f, 2f)
            };

            noise.OrientationNoise[2] = new NoiseSettings.NoiseParams
            {
                Amplitude = new Vector3(0.05f, 0.05f, 0.05f),
                Frequency = new Vector3(10f, 10f, 10f)
            };

            return noise;
        }
        */
    }
}
